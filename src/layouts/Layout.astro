<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>Astro Basics</title>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  /* 1. Configuration de base du site (ne change pas) */
  html,
  body {
    margin: 0;
    width: 100%;
    height: 100%;
  }

  /* 2. CORRECTIF GOOGLE TRANSLATE (Le "Mur en béton") */

  /* On force html ET body à rester collés en haut à 0px */
  html,
  body {
    top: 0 !important;
    margin-top: 0 !important;
    position: relative !important; /* Garde la structure stable */
  }

  /* On cache l'iframe (la barre bleue) de Google */
  .goog-te-banner-frame.skiptranslate {
    display: none !important;
  }

  /* On cache l'espace vide que Google essaie parfois de réserver */
  body > .skiptranslate {
    display: none !important;
  }

  /* On cache le logo Google et les popups inutiles */
  .goog-logo-link {
    display: none !important;
  }
  .goog-te-gadget {
    color: transparent !important;
  }
</style>
